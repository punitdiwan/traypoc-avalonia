# Vite dev-server proxy target.
# All /api/* requests are forwarded here (the /api prefix is stripped).
# Change this if your API runs on a different host or port.
# In production, nginx proxies /api to the API server — this file is dev-only.
VITE_API_TARGET=http://localhost:8080
